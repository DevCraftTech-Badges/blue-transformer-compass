
import React from 'react';
import { TransformerRelocationPage } from '@/components/transformerRelocation/TransformerRelocationPage';
import Layout from '@/components/layout/Layout';

export default function TransformerRelocation() {
  return (
    <Layout>
      <TransformerRelocationPage />
    </Layout>
  );
}
